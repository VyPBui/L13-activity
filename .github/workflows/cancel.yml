name: Cancel
on: [push]
jobs:
  cancel:
    name: 'Cancel Previous Runs'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: styfle/cancel-workflow-action
        with:
          workflow_id: 479426